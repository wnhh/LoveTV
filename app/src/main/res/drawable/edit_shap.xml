<?xml version="1.0" encoding="utf-8"?>
    <shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="line">
    <solid android:color="@color/talk" />
    <padding
        android:left="2dp"
        android:right="2dp"
        android:top="30dp" />
    <solid android:color="@color/talk" />
    <stroke
        android:width="1dp"
        android:color="@color/talk" />
    <size
        android:height="15dp"
        android:width="300dp" />
</shape>
